{% extends 'base.html.twig' %}

{% block title %}Usercateg index{% endblock %}

{% block body %}
    <h1>Usercateg index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Label</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for usercateg in usercategs %}
            <tr>
                <td>{{ usercateg.id }}</td>
                <td>{{ usercateg.label }}</td>
                <td>
                    <a href="{{ path('usercateg_show', {'id': usercateg.id}) }}">show</a>
                    <a href="{{ path('usercateg_edit', {'id': usercateg.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('usercateg_new') }}">Create new</a>
{% endblock %}
